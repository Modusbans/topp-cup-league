import React from "react";

const NewsSlider = () => (
  <section id="newsslider">
    <h2 className="text-2xl font-bold mb-2 text-blue-800">NewsSlider</h2>
    <p>Contenuto della sezione NewsSlider...</p>
  </section>
);

export default NewsSlider;
